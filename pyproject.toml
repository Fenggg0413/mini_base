[project]
name = "mini_base"
version = "1.0.0"
description = "A miniature relational database management system for educational purposes"
requires-python = ">=3.8"
dependencies = [
    "ply>=3.11",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]